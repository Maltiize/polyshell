/*
Classe abstraite : Une classe abstraite est une classe impossible a instancier. Donc utilisé lorsque la classe Mère ne peut pas être instancier;

Interface : Une classe qui est a 100% abstract; Utilise le polymorphisme

Classe : permet de décrire un objet qu'il sera possible généralement d'instancier et qui peut posseder des attributs des méthodes

Private : pour éviter que l'utilisateur les utilise en dehors de la classe. 
Static : variable de classe 

throws : prevenir qu'il y aura peut etre une erreur;
throw : lancer une exception;


La généricité permet de réutiliser sans risque le polymorphisme avec les collections.

*/


